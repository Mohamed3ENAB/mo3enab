-- ============================================================
--  ترحيل ٠٠٤ — إخفاء رقم العميل على لوحة الطلبات
--
--  دليل السواقين والمحلات بيعرض الأرقام عن قصد — دي الخدمة نفسها.
--  إنما صاحب الطلب ده مواطن عادي بينشر رقمه على صفحة مفتوحة، وده
--  شيء تاني خالص. دلوقتي يقدر يخفيه ويخلي الكلام جوه التطبيق.
--
--  التشغيل: psql -d <db> -v ON_ERROR_STOP=1 -f db/migrate-004-privacy.sql
-- ============================================================

alter table requests add column if not exists hide_phone boolean not null default false;
